# render.yaml completo y funcional para Render.com
# Ajustado para Flask con Gunicorn, Python 3.10

services:
  - type: web
    name: tu-nombre-de-servicio
    env: python
    buildCommand: ""
    startCommand: gunicorn main:app
    pythonVersion: 3.10.12
